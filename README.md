
# Basic Express App

Express.js is a Node.js framework used to build web applications and APIs.It is an NPM package, meaning it can be installed using Node Package Manager (NPM).It helps manage server-side logic, making it easier to handle requests and responses.


## Setting up
To set up basic app you need to install some packages by running commands in terminal.

1 . Install node.js.

2 . Initialize project by running (npm init -y) in terminal

3 . Install express.js
          
    npm install express

## Installation

Install my-project with 

```bash
  git clone https://github.com/hareem09/server-express
```
Change directory by running

```bash
cd server-express
```
    
## Usage
Start the server
```bash
node express.js
```
The server will be running at port:
```bash
http://localhost:5000
```


## API Endpoints

Want to visit on different routes run:

Demo:

  will go to about page

    http://localhost:5000/about 

want to go ot contact page:
   
     http://localhost:3000/contact

just need to change the end part .
## Error Handling
If there is error in internal server it will return status 500 which will display internal server error message on screen
